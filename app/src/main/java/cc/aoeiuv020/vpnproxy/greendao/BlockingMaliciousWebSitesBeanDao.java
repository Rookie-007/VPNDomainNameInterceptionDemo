package cc.aoeiuv020.vpnproxy.greendao;

import android.database.Cursor;
import android.database.sqlite.SQLiteStatement;

import org.greenrobot.greendao.AbstractDao;
import org.greenrobot.greendao.Property;
import org.greenrobot.greendao.internal.DaoConfig;
import org.greenrobot.greendao.database.Database;
import org.greenrobot.greendao.database.DatabaseStatement;

import cc.aoeiuv020.vpnproxy.greendao.bean.BlockingMaliciousWebSitesBean;

// THIS CODE IS GENERATED BY greenDAO, DO NOT EDIT.
/** 
 * DAO for table "BLOCKING_MALICIOUS_WEB_SITES_BEAN".
*/
public class BlockingMaliciousWebSitesBeanDao extends AbstractDao<BlockingMaliciousWebSitesBean, Long> {

    public static final String TABLENAME = "BLOCKING_MALICIOUS_WEB_SITES_BEAN";

    /**
     * Properties of entity BlockingMaliciousWebSitesBean.<br/>
     * Can be used for QueryBuilder and for referencing column names.
     */
    public static class Properties {
        public final static Property Id = new Property(0, Long.class, "id", true, "_id");
        public final static Property WebsiteUrl = new Property(1, String.class, "websiteUrl", false, "WEBSITE_URL");
    }


    public BlockingMaliciousWebSitesBeanDao(DaoConfig config) {
        super(config);
    }
    
    public BlockingMaliciousWebSitesBeanDao(DaoConfig config, DaoSession daoSession) {
        super(config, daoSession);
    }

    /** Creates the underlying database table. */
    public static void createTable(Database db, boolean ifNotExists) {
        String constraint = ifNotExists? "IF NOT EXISTS ": "";
        db.execSQL("CREATE TABLE " + constraint + "\"BLOCKING_MALICIOUS_WEB_SITES_BEAN\" (" + //
                "\"_id\" INTEGER PRIMARY KEY ," + // 0: id
                "\"WEBSITE_URL\" TEXT);"); // 1: websiteUrl
    }

    /** Drops the underlying database table. */
    public static void dropTable(Database db, boolean ifExists) {
        String sql = "DROP TABLE " + (ifExists ? "IF EXISTS " : "") + "\"BLOCKING_MALICIOUS_WEB_SITES_BEAN\"";
        db.execSQL(sql);
    }

    @Override
    protected final void bindValues(DatabaseStatement stmt, BlockingMaliciousWebSitesBean entity) {
        stmt.clearBindings();
 
        Long id = entity.getId();
        if (id != null) {
            stmt.bindLong(1, id);
        }
 
        String websiteUrl = entity.getWebsiteUrl();
        if (websiteUrl != null) {
            stmt.bindString(2, websiteUrl);
        }
    }

    @Override
    protected final void bindValues(SQLiteStatement stmt, BlockingMaliciousWebSitesBean entity) {
        stmt.clearBindings();
 
        Long id = entity.getId();
        if (id != null) {
            stmt.bindLong(1, id);
        }
 
        String websiteUrl = entity.getWebsiteUrl();
        if (websiteUrl != null) {
            stmt.bindString(2, websiteUrl);
        }
    }

    @Override
    public Long readKey(Cursor cursor, int offset) {
        return cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0);
    }    

    @Override
    public BlockingMaliciousWebSitesBean readEntity(Cursor cursor, int offset) {
        BlockingMaliciousWebSitesBean entity = new BlockingMaliciousWebSitesBean( //
            cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0), // id
            cursor.isNull(offset + 1) ? null : cursor.getString(offset + 1) // websiteUrl
        );
        return entity;
    }
     
    @Override
    public void readEntity(Cursor cursor, BlockingMaliciousWebSitesBean entity, int offset) {
        entity.setId(cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0));
        entity.setWebsiteUrl(cursor.isNull(offset + 1) ? null : cursor.getString(offset + 1));
     }
    
    @Override
    protected final Long updateKeyAfterInsert(BlockingMaliciousWebSitesBean entity, long rowId) {
        entity.setId(rowId);
        return rowId;
    }
    
    @Override
    public Long getKey(BlockingMaliciousWebSitesBean entity) {
        if(entity != null) {
            return entity.getId();
        } else {
            return null;
        }
    }

    @Override
    public boolean hasKey(BlockingMaliciousWebSitesBean entity) {
        return entity.getId() != null;
    }

    @Override
    protected final boolean isEntityUpdateable() {
        return true;
    }
    
}
